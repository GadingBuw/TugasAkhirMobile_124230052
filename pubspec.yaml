name: library_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1               # Untuk melakukan request API
  provider: ^6.1.2            # Untuk state management
  shared_preferences: ^2.2.3  # Untuk menyimpan session login
  crypto: ^3.0.3              # Untuk hashing password
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.3 # Untuk menemukan path penyimpanan

  geolocator: ^14.0.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.1

  # flutter_local_notifications: ^17.2.3
  # timezone: ^0.9.4
  # permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.9

flutter:
  uses-material-design: true

  assets:
    - assets/images/
